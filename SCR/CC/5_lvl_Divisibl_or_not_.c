#include <stdio.h>
int main()
{
    // divisible or not
    int a = 3967;
    int b = a % 3;
    printf("the value of a is %d ", a%3);
        // % is a remainder function if comes zero(0) than it is divisible else
    return 0;
}